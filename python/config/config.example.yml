# 复制为 config.yml 后按需修改；密钥请优先写在 .env（见 .env.example）
ai:
  chat:
    temperature: 0.7
    history_limit: 10
  execute:
    model: "qwen-plus"
    api_key: ""
    temperature: 0.0
    base_url: "https://dashscope.aliyuncs.com/compatible-mode/v1"

mysql:
  host: localhost
  port: 3306
  user: root
  password: ""
  database: myblog

redis:
  host: localhost
  port: 6379
  db: 5
  password: ""
  chat_list_key: "chat:s:{session_id}"
  chat_ttl: 86400
  max_cache_msgs: 40
