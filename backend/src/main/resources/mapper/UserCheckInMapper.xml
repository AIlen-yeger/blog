<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.personalblog.mapper.UserCheckInMapper">

    <insert id="insertIgnore">
        INSERT IGNORE INTO user_check_in (user_id, check_in_date)
        VALUES (#{userId}, #{checkInDate})
    </insert>

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM user_check_in WHERE user_id = #{userId}
    </select>

    <select id="existsByUserIdAndDate" resultType="int">
        SELECT COUNT(*) FROM user_check_in
        WHERE user_id = #{userId} AND check_in_date = #{checkInDate}
    </select>

    <select id="selectDatesByUserIdSince" resultType="string">
        SELECT DATE_FORMAT(check_in_date, '%Y-%m-%d') AS d
        FROM user_check_in
        WHERE user_id = #{userId}
          AND check_in_date &gt;= #{since}
        ORDER BY check_in_date ASC
    </select>
</mapper>
