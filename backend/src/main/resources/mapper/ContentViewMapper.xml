<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.personalblog.mapper.ContentViewMapper">

    <insert id="insertIgnore">
        INSERT IGNORE INTO content_views (content_type, content_id, viewer_key)
        VALUES (#{contentType}, #{contentId}, #{viewerKey})
    </insert>

    <delete id="deleteByContent">
        DELETE FROM content_views
        WHERE content_type = #{contentType} AND content_id = #{contentId}
    </delete>
</mapper>
